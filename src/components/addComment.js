import React, { Component } from 'react';
import { withStyles } from '@material-ui/core/styles';

const styles = theme => ({})

class AddComment extends Component {

  render () {
    return <div>hello add comment</div>
  }
}

export default withStyles(styles)(AddComment)